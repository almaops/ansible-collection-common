//
// Do any local configuration here
//

// Consider adding the 1918 zones here, if they are not used in your
// organization
//include "/etc/bind/zones.rfc1918";

// Zones by ansible role
{% for zone in bind9_zones %}
zone "{{ zone.name }}" {
    type {{ zone.type }};
    file "{{ zone.file }}";
};
{% endfor %}

{{ bind9_config_local_raw }}